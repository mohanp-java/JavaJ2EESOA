SELECT  * FROM ITEMS ;

INSERT INTO ITEMS 
values(10, 'Item 1', 3434.45);

UPDATE ITEMS 
SET NAME = 'Fitbit Ionic'
WHERE NAME = 'Item 1';

SELECT * FROM ITEMS i ;

SELECT * FROM ITEMS i  
WHERE ITEMID = 10;

DELETE FROM  ITEMS 
WHERE ITEMID = 10;

TRUNCATE TABLE ITEMS ;

create or replace PROCEDURE add(a in int, b in int)
return int as
begin
  return a + b;
end;

SET SERVEROUTPUT ON;

CREATE OR REPLACE PROCEDURE CALCULATE_PRICE (PRICE IN NUMBER,QUANTITY IN NUMBER,TOTAL OUT NUMBER)
IS
BEGIN
TOTAL := PRICE * QUANTITY;
END;